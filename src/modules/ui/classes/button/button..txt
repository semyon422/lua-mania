class uiObject
	name, x/y, x/yAlign, ox/y, r(otate), info, contextmenu

	class input
		disabled, readonly, value
		
		type button
		type checkbox
		type radio
		type text
			placeholder
		type select
			type multiple/dropdown, options